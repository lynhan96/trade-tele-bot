/** Coins that run BOTH INTRADAY and SWING strategies simultaneously. */
export const DUAL_TIMEFRAME_COINS = ['BTC', 'ETH', 'SOL', 'BNB', 'XRP'];

/** Fixed Grid DCA levels — deviation from entry (%). */
export const GRID_DEVIATIONS = [0, 2, 4, 6];

/** Fixed Grid DCA weights — % of total notional per level. */
export const DCA_WEIGHTS = [40, 15, 15, 30];
